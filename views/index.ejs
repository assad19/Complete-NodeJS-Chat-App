<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Interface</title>
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
        }
        .chat-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
            background: linear-gradient(to bottom, #E6E6FA, #8A2BE2);
        }
        .chat-window {
            width: 100%;
            max-width: 400px;
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .chat-header {
            background: #8A2BE2;
            color: white;
            padding: 10px;
            text-align: center;
        }
        .chat-messages {
            height: 300px;
            overflow-y: auto;
            padding: 10px;
        }
        .message {
            max-width: 80%;
            margin-bottom: 10px;
            padding: 8px;
            border-radius: 8px;
        }
        .receive-message {
            background: #f0f0f0;
            align-self: flex-start;
        }
        .send-message {
            background: #E6E6FA;
            align-self: flex-end;
            margin-left: auto;
        }
        .chat-input {
            display: flex;
            padding: 10px;
            border-top: 1px solid #e0e0e0;
        }
        .chat-input input {
            flex-grow: 1;
            padding: 8px;
            border: 1px solid #e0e0e0;
            border-radius: 4px;
        }
        .chat-input button {
            padding: 8px 16px;
            background: #8A2BE2;
            color: white;
            border: none;
            border-radius: 4px;
            margin-left: 8px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="chat-container">
        <div class="chat-window">
            <div class="chat-header">
                &#128222;
            </div>
            <div class="chat-messages" id="chat-messages">
              <!-- <div class="message send-message">I have a question about my account.</div> -->
              <!-- <div class="message receive-message">Hello! How can I help you today?</div> -->
            </div>
            <div class="chat-input">
              <form id="submitForm">
                <input id="inputMessage" type="text" placeholder="Type your message...">
                <button type="submit">Send</button>
              </form>
            </div>
        </div>
    </div>
</body>
<script src="socket.io/socket.io.js"></script>
<script src="../client.js"></script>
</html>